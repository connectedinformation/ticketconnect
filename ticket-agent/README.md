# ticket-agent

Unprivileged. Performs X25519MLKEM768 handshakes (OpenSSL 3.5), pools TLS 1.3 resumption sessions per destination, and serves them (with PQC provenance) over a node-local Unix socket.
