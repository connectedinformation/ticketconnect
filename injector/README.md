# injector

Privileged. eBPF CO-RE uprobes for detection/timing; ptrace performs the injection via PUBLIC OpenSSL APIs only (remote d2i_SSL_SESSION + SSL_set_session) — never writes private struct fields.
